###### 编码

```
1、ascii    1个字节
2、unicode  至少两个字节
3、utf-8	   可变长编码
```

###### 转义字符

```
转义符	说明
\ 	(在行尾时)	续行符
\\	反斜杠符号
\'		单引号
\"		双引号
\a	响铃
\b	退格(Backspace)
\0	空
\n		换行
\t	横向制表符
\r 	回车
\0yy		八进制数，yy代表的字符，例如：\012代表换行
\xyy		十六进制数，yy代表的字符，例如：\x0a代表换行
```

###### 原始字符

```
In [7]: print('aaa\n')
aaa


In [8]: print(r'aaa\n')
aaa\n

\不能放在最后
In [10]: r'a\'
  File "<ipython-input-10-3ad28790edb2>", line 1
    r'a\'
         ^
SyntaxError: EOL while scanning string literal

```

###### 编码解码

```
编码 把字符串转换成字节 
In [16]: s.encode('utf8')
Out[16]: b'aa'

解码 把字节转换成字符串
In [18]: s = b'aa'
In [19]: s.decode('utf8')
Out[19]: 'aa'
```

**注意**

​	gbk中一个中文字符占2个长度

​	utf8中一个中文字符占3个长度

```
In [25]: s = '你好'

In [26]: len(s)
Out[26]: 2

In [27]: len(s.encode('utf8'))
Out[27]: 6

In [28]: len(s.encode('gbk'))
Out[28]: 4
```

###### 字符串格式化

```
占位符	替换内容
%d	   十进制整数 
%i	   整数
%u	   无符号整数
%o	   八进制整数
%x %X  十六进制整数
%g G   浮点数取整
%f e E 浮点数 .5f .2f
%s	   字符串
%c     字符
%x	   十六进制整数
%r	   原始字符串  如果要保留字符串里面的原始内容可以用 %r 而不是 %s
%%     %


#-*- coding:utf-8 -*-
# 字符串格式化  format
'''

格式	说明
{}	字符串形式
{索引}	按位置
{name}	按名称
{集合位置[索引]}	操作集合的元素
{:b}{:d}{:o}{:x}{:.3f}	不同进制的整数、浮点数
{:,}	千分位 1,234,567,890
{:>8}	^、<、>分别是居中、左对齐、右对齐，后面带宽度和一个填充字符，默认按空格填充
{!r}{!s}	{!r}原样输入，自动加上引号。    {!s}  转义输出 "{!r} {!s}".format('name','name')
'''
print(" {!r} {!s}".format('test1', 'test2'))        {!r} 原样输出，而且保留引号
print(" {!r} {!s}".format('te\nst1', 'tes\nt2'))    {!s} 转义输出，不保留引号

# 普通
In [38]: print('{},{}'.format('laopei', 18))
laopei,18

In [37]: print('{},{},{}'.format('laopei', 18))
---------------------------------------------------------------------------
IndexError                                Traceback (most recent call last)
<ipython-input-37-f089ebb394ad> in <module>()
----> 1 print('{},{},{}'.format('laopei', 18))

IndexError: tuple index out of range

# 位置
In [35]: print('{0},{0}'.format('laopei', 18))
laopei,laopei
In [36]: print('{0},{0},{1}'.format('laopei', 18))
laopei,laopei,18

# 关键字
In [34]: print('{name},{num}'.format(num = 100, name = 'shsxt'))
shsxt,100

# 对齐 < 左  >右 ^ 居中
In [39]: print("{:<8}".format(211))  # 默认空格填充，8位，靠左
211
In [40]: print("{:&<8}".format(211))   # 使用#填充
211&&&&&
In [41]: print("{:&<8}".format(211985211985))
211985211985

In [43]: print("{:>8}".format(211))  # 默认空格填充，8位，靠右
     211
In [44]: print("{:&>8}".format(211))
&&&&&211
In [45]: print("{:&>8}".format(211985211985))
211985211985

In [46]: print("{:^8}".format(211))  # 默认空格填充，8位，居中
  211
In [47]: print("{:&^8}".format(211))
&&211&&&
In [48]: print("{:&^8}".format(211985211985))
211985211985

# 小数 千分位
In [49]: print('{:,}'.format(123123))  # ,千分位
123,123
In [50]: print('{:.2f}'.format(123123.7698))  # 保留2位小数
123123.77
In [51]: print('{:,.2f}'.format(123123.7698))
123,123.77

# 进制制
In [52]: print(' int: {0:d};  hex: {0:x};  oct: {0:o};  bin: {0:b}'.format(47))  # 进制
 int: 47;  hex: 2f;  oct: 57;  bin: 101111
# 列表的元素
In [53]: print('{0[0]},{0[1]}'.format(['shsxt', 18]))  # 访问列表的元素
shsxt,18
In [54]: print('{per[0]},{per[1]}'.format( per = ['shsxt', 18]))  # 访问列表的元素
shsxt,18

# 自定义  -->了解
In [55]: class Book:
    ...:     def __init__(self, name, price):
    ...:         self.name, self.price = name, price
    ...:     def __str__(self):
    ...:         return '书名是:{xxxx.name},价格为:{xxxx.price}'.format(xxxx=self) # 访问属性
    ...: print(str(Book('python入门', 88)))
    ...:
    ...:
书名是:python入门,价格为:88

```

###### 切片

与列表相同

**注意 ：**

​	***字符串为不可改变类型， s = shsxt, s1 = s[:] 或者s[0:] 或者 s[0: 最后 +1] 中s s 和 s1 是指向同一个引用, , 而其他的切片都会重新分配内存**

###### 常用API

```
1、count('sub')  # 统计子串出现的次数

2、find('sub')   # 查找第一次出现的位置 不存在返回-1

	index(sub[,s tart[,end]]): 类似find()，不同在于如果sub不在字符串中，返回的不是-1而是异常

3、split(',') : 分割  ，可能多个 , 不保留切割符

	splitlines()  按行分割

4、parititon() : 分割 ,返回分割符左边的、分隔符、和右边的 三个值。如果没有，返回两个空串及源串。

5、strip() 删除字符串两头的符号，默认是空格

```

