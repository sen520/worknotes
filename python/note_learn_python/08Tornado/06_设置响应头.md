#### 设置响应头 ####
```
#coding=utf-8



import tornado.web
import tornado.ioloop


class IndexHandler(tornado.web.RequestHandler):
    def get(self, *args, **kwargs):
        #渲染文件
        # self.render('templates/login.html')
        with open('templates/login.html') as fr :
            content = fr.read()
        self.write(content)

class LoginHandler(tornado.web.RequestHandler):
    def post(self, *args, **kwargs):
        #获取请求参数
        account = self.get_argument('account')
        password = self.get_argument('password')
        # self.write(account+","+password)

        if account=='zhangsan' and password=='123':
            # self.redirect('https://www.baidu.com')
            self.set_status(302)
            self.set_header('Location','https://taobao.com')

        else:
            self.redirect('/')
app = tornado.web.Application([
    (r'/',IndexHandler),
    (r'/login/',LoginHandler)
])

app.listen(8000)
tornado.ioloop.IOLoop.instance().start()

```

```
#设置响应头：
set_header('Content-Type','image/png')

#获取所有的请求报文
self.request.headers
self.request.headers['User-Agent']


#Cookie
#普通Cookie操作
set_cookie(name='',value='',path='',expires_days=3)
get_cookie(name='')
clear_cookie(name='')

#加密Cookie操作
set_secure_cookie(name='',value='')
get_secure_cookie(name='')

app = tornado.web.Application([(r'',IndexHandler)],cookie_secret='fdsadfs')








```